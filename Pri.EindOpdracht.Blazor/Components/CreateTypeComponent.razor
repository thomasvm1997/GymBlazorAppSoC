<div class="card">
    <div class="card-header">Create Workout Type</div>
    <div class="card-body">
        <EditForm Model="Model" OnValidSubmit="HandleCreate">
            <DataAnnotationsValidator />

            <div class="mb-3">
                <label for="Achieved" class="form-label">Name</label>
                <InputText id="Achieved" class="form-control" @bind-Value="Model.Name" />
                <ValidationMessage For="@(() => Model.Name)" />
            </div>

            <div class="mb-3">
                <label for="Description" class="form-label">Description</label>
                <InputTextArea id="Description" class="form-control" @bind-Value="Model.Description" />
                <ValidationMessage For="@(() => Model.Description)" />
            </div>

            <div class="mb-3">
                <label for="TargetDate" class="form-label">Difficulty</label>
                <InputText id="TargetDate" class="form-control" @bind-Value="Model.Difficulty" />
                <ValidationMessage For="@(() => Model.Difficulty)" />
            </div>

            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-primary">Save Changes</button>
                <button type="button" class="btn btn-secondary" @onclick="HandleCancel">Cancel</button>
            </div>
        </EditForm>
    </div>
</div>

@code {
    [Parameter] public WorkoutTypeRequestDtoModel Model { get; set; }
    [Parameter] public EventCallback OnCreate { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private async Task HandleCreate()
    {
        await OnCreate.InvokeAsync();
    }
    private async Task HandleCancel()
    {
        await OnCancel.InvokeAsync();
    }
}
